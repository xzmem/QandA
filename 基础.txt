
byte 字节 = 8bite 位
short 16bite ==char 2字节 一个汉字

整型基础类型

byte  short int long
8bite 16    32  64

字符型基础类型
char 汉字 2byte 字节 16位bite

浮点型基础类型

float
小数后加f为float类型 32位 4字节
double
小数默认为double类型  64位 8字节

布尔型

boolean  true false 长度1 真正存放的是 0 1 但不能赋值

字符串类型---存放字符串常量池中



排序算法：1.把小的 往前固定位放
          2.把大的往后放 两两比较后移
		  
方法的重载：同一个类里面 方法名相同 参数不同

构造方法：默认无参构建 无返回值 构造器

this：表示本对象 

变量：基本参数变量，类变量

变量为基础类型时，=为赋值
变量为类类型时，=为引用指向
传参：基本类型传参是值传递
	  类类型传参 是引用传递

		  
成员变量 访问修饰符
 private

default
 protected
public 

单利模式：使构造器私有化无法调用，类内部实例唯一一个对象，然后返回这唯一对象的引用。 


重载： 同类中，方法名相同，参数不同，返回值 无要求

重写：子类继承父类方法，方法名相同，参数相同，返回值类型小于父类，限制小于父类


抽象类：被继承，子类继承一个，可有抽象方法，也可而无，不能被实例化
接口：实现，子类可实现多个，必须是抽象方法，声明的属性 是final 静态的 pubic

默认方法；jdk8 接口可以有方法实现

封装类型：对应的基础类型









 